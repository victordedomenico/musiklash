{
  "name": "@klash/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^7.7.0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7.7.0",
    "tsx": "^4.21.0"
  }
}
