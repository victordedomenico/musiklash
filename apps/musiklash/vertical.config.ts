/**
 * Re-export the full vertical config for app-local scripts or typing.
 * Next.js config uses the slug only via `createKlashNextConfig("musiklash")`.
 */
export { musiklash as default, musiklash } from "@klash/klash-config/configs/musiklash";
