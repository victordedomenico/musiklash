/**
 * Re-export the full vertical config for app-local scripts or typing.
 * Next.js config uses the slug only via `createKlashNextConfig("animeklash")`.
 */
export { animeklash as default, animeklash } from "@klash/klash-config/configs/animeklash";
